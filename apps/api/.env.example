DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
UNIVERSITY_SALT="examonchain-uni-salt-change-me"
PERSONALIZATION_SECRET="examonchain-personalization-secret"
PORT=4000
CORS_ORIGIN="http://localhost:3000"

# Relayer (same as deployer or dedicated key) — must have Sepolia ETH
RELAYER_PRIVATE_KEY="0x..."

# After forge create — optional; anchoring skipped if unset
EXAM_REGISTRY_ADDRESS=""
SCORE_ANCHOR_ADDRESS=""

# Optional: Alchemy Sepolia RPC
RPC_URL="https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY"

# AI question extraction (preferred — same as chain-guard OpenRouter)
OPENROUTER_API_KEY=""
# Optional overrides
# OPENROUTER_MODEL="google/gemini-2.0-flash-001"
# OPENROUTER_HTTP_REFERER="http://localhost:3000"
# OPENROUTER_APP_TITLE="ExamOnchain"

# Legacy fallback
OPENAI_API_KEY=""
