# MCP context for ExamOnchain

Cursor MCP: point `@modelcontextprotocol/server-filesystem` at this directory (see repo `.cursor/mcp.json`).

Read order: `00-overview.md` → `01-data-model.md` → others by task.
